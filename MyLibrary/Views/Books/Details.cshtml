@model MyLibrary.Models.BorrowViewModel.BorrowDetailViewModel

@{
    ViewData["Title"] = "Details";
}

<h1>Book Details</h1>

<div>
    <h4>Book</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Book.Title)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Book.Title)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Book.Language)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Book.Language)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Book.PublishDate)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Book.PublishDate)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Book.User)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Book.User.FullName)
        </dd>
        @* <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Book.catagory)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Book.catagory.CategoryId)
            </dd>*@
    </dl>
</div>
<hr />
@if (Model.IsBorrowed)
{
    <div>
        <h3> The book is not available to borrow </h3>
    </div>
}
else
{
    <form asp-controller="Borrows" asp-action="Create" asp-route-id="@Model.Book.BookId">
        <input type="submit" value="Borrow A Book" class="btn btn-primary" />
    </form>
}
<hr />
@if (Model.IsWishListed)
{
    <div>
        <h3> The book is already in your wish list </h3>
    </div>
}
else
{
    <form asp-controller="WishLists" asp-action="Create" asp-route-id="@Model.Book.BookId">
        <input type="submit" value="Wish List A Book" class="btn btn-primary" />
    </form>
}
<div>
    <a asp-action="Types">Back to List</a>
</div>
@* <div>
        <form asp-controller="Borrows" asp-action="Create" asp-route-id="@Model.BookId">
            <input type="submit" value="Borrow A Book" class="btn btn-primary" />

            <a asp-action="Edit" asp-route-id="@Model.BookId"></a>
        </form>
    </div>
    <hr />
    <div>
        <form asp-controller="WishLists" asp-action="Create" asp-route-id="@Model.BookId">
            <input type="submit" value="Wish List A Book" class="btn btn-primary" />
        </form>
    </div>
    <hr />*@
@* <div>
        <form asp-controller="Borrows" asp-action="Delete" asp-route-id="@Model.BookId">
            <input type="submit" value="Return A Book" class="btn btn-primary" />

            <a asp-action="Edit" asp-route-id="@Model.BookId"></a>
        </form>
    </div>*@

